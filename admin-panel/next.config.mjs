/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "localhost", "example.com", "cdn-icons-png.flaticon.com"],
  },
  // Other Next.js configuration options can go here
};

export default nextConfig;
