export const metadata = {
  title: "Login - ServMaster",
  description: "Login page for Admins of ServMaster",
};

export default function LoginLayout({ children }) {
  return <main>{children}</main>;
}
